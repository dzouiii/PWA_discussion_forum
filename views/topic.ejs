<!DOCTYPE html>
<html>
<head>
    <title>Topics page</title>
    <link rel="stylesheet" href="//netdna.bootstrapcdn.com/bootstrap/3.0.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="//netdna.bootstrapcdn.com/font-awesome/4.0.3/css/font-awesome.min.css">
    <style>
        body        { padding-top:50px; word-wrap:break-word; }
    </style>
</head>
<body>
<nav class="navbar navbar-inverse navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">
            <a class="navbar-brand" href="#">PWA discussion forum</a>
        </div>
    </div>
</nav>

<div class="container">

    <div class="page-header text-center">
        <h1><span class="fa fa-anchor"></span> Topics</h1>
        <p><a href="/response/<%= topic._id %>" class="btn btn-default btn-sm">Response</a></p>
    </div>

    <!-- TOPIC -->
    <div class="row">
        <div class="container">
            <div class="well">
                <h3 style="color:black"><span style="color:black" class="fa fa-comment"></span> <%= topic.topic.title %></h3></a>
                <strong>Text</strong>: <%= topic.topic.text %> <br>
                <strong>Created by</strong>: <%= topic.topic.author %> <%= topic.topic.date %>  <br>
            </div>
        </div>
    </div><br>

    <!-- RESPONSE -->
    <% if (responses.length > 0) { %>
    <% for(var i=0; i<responses.length; i++) {%>
    <div class="row">
        <div class="container">
            <div class="well">
                <h3 style="color:black"><span style="color:black" class="fa fa-comment"></span> User: <%= responses[i].response.author %></h3></a>
                <strong>Response</strong>: <%= responses[i].response.text %> <br>
                <strong>Created at</strong>: <%= responses[i].response.date %> <br>
            </div>
        </div>
    </div><br>
    <% } %>
    <% } %>

</div>
</body>
</html>