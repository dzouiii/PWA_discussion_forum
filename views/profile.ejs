<!DOCTYPE html>
<html>
<head>
    <title>Topics page</title>
    <link rel="stylesheet" href="//netdna.bootstrapcdn.com/bootstrap/3.0.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.5.0/css/font-awesome.min.css">
    <style>
        body        { padding-top:50px; word-wrap:break-word; }
    </style>
</head>
<body>
<nav class="navbar navbar-inverse navbar-fixed-top">
    <div class="container">
         <div class="navbar-header">
             <a class="navbar-brand" href="/profile">PWA discussion forum</a>
         </div>
    </div>
</nav>

<div class="container">

    <div class="page-header text-center">
        <h1><span class="fa fa-anchor"></span> Topics</h1>
        <p>Logged as:  <a href="/new_topic" class="btn btn-default btn-sm">New Topic</a></p>
    </div>



        <!-- TOPICS -->
        <% for(var i=0; i<topics.length; i++) {%>
            <div class="row">
                <div class="container">
                    <div class="well">
                        <a href="/topic/<%= topics[i]._id %>"><h3 style="color:black"><span style="color:black" class="fa fa-comment"></span> <%= topics[i].topic.title %></h3></a>
                        <strong>Text</strong>: <%= topics[i].topic.text %> <br>
                        <strong>Created by</strong>: <%= topics[i].topic.author %> <%= topics[i].topic.date %>  <br>
                        <% if (user.local.email == topics[i].topic.author) { %>
                        <a href="/delete_topic/<%= topics[i]._id %>"><h5><span class="fa fa-trash"></span> Delete</h5></a>
                        <% } %>
                    </div>
                </div>
    </div><br>
        <% } %>


</div>
</body>
</html>